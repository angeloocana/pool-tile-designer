{
  "name": "pool-tile-designer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ptz-assert": "^1.6.8",
    "ramda": "^0.24.1",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-intl": "^2.3.0",
    "react-scripts": "1.0.10",
    "styled-components": "^2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "rm -rf build && npm run build && surge build/"
  }
}
